apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: app-visits
  name: app-visits
  namespace: aulainfra
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app-visits
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  template:
    metadata:
      labels:
        app: app-visits
    spec:
      containers:
      - image: aulainfraacr.azurecr.io/visits
        imagePullPolicy: Always
        name: backend-visits
        env:
          - name: TZ
            valueFrom:
              configMapKeyRef:
                name: app-configmap
                key: TZ
          - name: MYSQL_URL
            value: jdbc:mysql://mysqldb-visits/visits
          - name: MYSQL_USER
            value: root
          - name: MYSQL_PASS
            value: root
        ports:
          - containerPort: 8082
        resources: {}
      restartPolicy: Always
      serviceAccountName: ""
status: {}
